<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.cit.bayonas.citmedconnect">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CitMedConnect"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.CitMedConnect.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.LandingActivity"
            android:exported="true"
            android:theme="@style/Theme.CitMedConnect.Auth" />

        <activity
            android:name=".ui.LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.CitMedConnect.Auth" />

        <activity
            android:name=".ui.RegisterActivity"
            android:exported="true"
            android:theme="@style/Theme.CitMedConnect.Auth" />

        <activity
            android:name=".ui.DashboardActivity"
            android:exported="true"
            android:theme="@style/Theme.CitMedConnect.Auth"
            android:windowSoftInputMode="adjustPan" />

        <activity
            android:name=".ui.AppointmentsActivity"
            android:exported="true"
            android:theme="@style/Theme.CitMedConnect.Auth"
            android:windowSoftInputMode="adjustPan" />

        <activity
            android:name=".ui.AdminAppointmentsActivity"
            android:exported="true"
            android:theme="@style/Theme.CitMedConnect.Auth"
            android:windowSoftInputMode="adjustPan" />

        <activity
            android:name=".ui.ProfileActivity"
            android:exported="false"
            android:theme="@style/Theme.CitMedConnect.Auth"
            android:windowSoftInputMode="adjustPan" />

        <activity
            android:name=".ui.MedicalRecordsActivity"
            android:exported="false"
            android:theme="@style/Theme.CitMedConnect.Auth"
            android:windowSoftInputMode="adjustPan" />

        <activity
            android:name=".ui.NotificationsActivity"
            android:exported="false"
            android:theme="@style/Theme.CitMedConnect.Auth"
            android:windowSoftInputMode="adjustPan" />

    </application>

</manifest>
